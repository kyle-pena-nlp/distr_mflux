{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --env-file=.dev.env --watch index.ts",
    "test": "bun --watch test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.6.10",
    "nats": "^2.28.2",
    "zod": "^3.23.8"
  }
}